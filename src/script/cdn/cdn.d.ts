export declare class MiniSearch {
  constructor(param: Record<string, string[]>);
  search(param: string, options?: Record<string, any>): any[];
  addAll(param: any[]): void;
}

export declare class Pikaday {
  constructor(param: Record<string, any>);
  /*
	gotoToday : () => void;
	show : () => void;
	toString : (params? : string) => string;
	gotoDate : (date : Date) => void;*/
}
